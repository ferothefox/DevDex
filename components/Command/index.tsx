'use client'

export default () => {
  // const { status } = useSession()

  return (
    <>
      <div>b</div>
      {/* {status === 'authenticated' && process.env.NODE_ENV === 'production' ? (
        <AuthenticatedMenu />
      ) : (
        status === 'unauthenticated' &&
        process.env.NODE_ENV !== 'production' && <AuthenticatedMenu />
      )} */}
    </>
  )
}
